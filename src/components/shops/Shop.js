import React from 'react'
import PropTypes from 'prop-types'

const Shop = ({ shopId }) => {
    return (
        <>
        </>
    )
}

Shop.propTypes = {
    shopId: PropTypes.string.isRequired
}

export default Shop
